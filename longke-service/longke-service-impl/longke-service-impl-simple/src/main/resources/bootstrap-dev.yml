# Spring
spring:
  cloud:
    nacos:
      config:
        server-addr: 139.224.55.226:8848
        file-extension: yaml
      discovery:
        server-addr: 139.224.55.226:8848
  datasource:
    dynamic:
      primary: master # 设置默认的数据源或者数据源组,默认值即为master
      strict: false # 严格匹配数据源,默认false. true未匹配到指定数据源时抛异常,false使用默认数据源
      datasource:
        master:
          url: jdbc:mysql://139.224.55.226:3306/simple-service
          username: HuWeilong
          password: Alex-2713-passwd
          driver-class-name: com.mysql.cj.jdbc.Driver
        slave:
          url: jdbc:mysql://139.224.55.226:3306/simple-service
          username: HuWeilong
          password: Alex-2713-passwd
          driver-class-name: com.mysql.cj.jdbc.Driver

xxl:
  job:
    admin:
      # 调度中心部署根地址 [选填]：如调度中心集群部署存在多个地址则用逗号分隔。执行器将会使用该地址进行"执行器心跳注册"和"任务结果回调"；为空则关闭自动注册；
      addresses: http://139.224.55.226:10001/
    # 执行器通讯TOKEN [选填]：非空时启用；
    accessToken: Alex-2713-passwd
    executor:
      # 执行器AppName [选填]：执行器心跳注册分组依据；为空则关闭自动注册
      appname: simple-service
      # 执行器注册 [选填]：优先使用该配置作为注册地址，为空时使用内嵌服务 ”IP:PORT“ 作为注册地址。从而更灵活的支持容器类型执行器动态IP和动态映射端口问题。
      address:
      # 执行器IP [选填]：默认为空表示自动获取IP，多网卡时可手动设置指定IP，该IP不会绑定Host仅作为通讯实用；地址信息用于 "执行器注册" 和 "调度中心请求并触发任务"；
      ip:
      # 执行器端口号 [选填]：小于等于0则自动获取；默认端口为9999，单机部署多个执行器时，注意要配置不同执行器端口；
      port: 0
      # 执行器日志路径 [选填]：需要对该路径拥有读写权限；为空则使用默认路径；
      logpath:
       # 执行器日志保存天数 [选填]：值大于3时生效，启用执行器Log日志文件保存，否则关闭；为空则使用默认值3；
      logretentiondays: 30